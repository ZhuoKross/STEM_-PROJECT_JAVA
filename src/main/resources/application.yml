server:
  port: 8081
  ssl:
    enabled: false

spring:
  application:
    name: stemProyect

  datasource:
    url: jdbc:postgresql://localhost:5500/DBstem
    username: postgres
    password: "Jinwoo.2912_000"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect

  main:
    allow-circular-references: true
